# Postgres — na Railway, linke o serviço de Postgres ao Next e a variável é
# injetada automaticamente. Localmente, cole aqui a "Public Network" URL do
# banco (Railway → Postgres → Connect → Public Network).
DATABASE_URL=postgres://usuario:senha@host:5432/nome_do_banco
